from django.db import models

# This project is a static research-documentation site.
# No database-backed models are required — all content (pipelines,
# results, references) is defined directly in core/views.py as
# Python data structures and passed into templates as context.
